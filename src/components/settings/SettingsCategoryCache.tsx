import React from 'react';
// TODO: Import SettingSlider, SettingDropdown, ValidationBadge

type Props = object;

const SettingsCategoryCache: React.FC<Props> = () => {
  // TODO: intégrer store et logique
  return (
    <div>
      <h2 className="text-lg font-semibold mb-4">Gestion du cache</h2>
      {/* TODO: Sliders, dropdowns, boutons, affichage live */}
      <div className="text-neutral-400">À implémenter</div>
    </div>
  );
};

export default SettingsCategoryCache;
