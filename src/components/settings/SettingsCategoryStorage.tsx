import React from 'react';
// TODO: Import SettingFileInput, ValidationBadge

type Props = object;

const SettingsCategoryStorage: React.FC<Props> = () => {
  // TODO: intégrer store et logique
  return (
    <div>
      <h2 className="text-lg font-semibold mb-4">Emplacements de stockage</h2>
      {/* TODO: Inputs pour chaque chemin + bouton Browse + validation */}
      <div className="text-neutral-400">À implémenter</div>
    </div>
  );
};

export default SettingsCategoryStorage;
